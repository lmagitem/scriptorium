import { TranslatableString } from './translatable-string';

export interface TranslatableTitleAndDescription {
  /** This entry's title. */
  title: TranslatableString;
  /** This entry's description. */
  description: TranslatableString;
}
